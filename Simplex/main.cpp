#include <stdio.h>
#include <iostream>
#include <vector>
#include "Simplex.h"
using namespace std;


int main(void)
{
	/* get input data */
		/* Solving cT[N_standart] * x[N_standart] -> min */
	/* Initialize input matrix A[M,N_standart] */
	/* First step - solve sum(y[i]) -> min, y[M] >= 0 */
	/* Initialize expanded input matrix A[M,N_expanded] */
	
	/* Setting starting vec*/
	

	/* Created L_k */
	readFile();
	return 0;
}